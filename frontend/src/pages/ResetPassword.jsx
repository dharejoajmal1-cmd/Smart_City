import { useState } from 'react';import { Link,useSearchParams,useNavigate } from 'react-router-dom';import Button from '../components/common/Button';import authService from '../api/authService';import useToast from '../hooks/useToast';
export default function ResetPassword(){const [params]=useSearchParams();const nav=useNavigate();const toast=useToast();const [password,setPassword]=useState('');const [loading,setLoading]=useState(false);const submit=async e=>{e.preventDefault();setLoading(true);try{await authService.resetPassword({email:params.get('email'),token:params.get('token'),newPassword:password});toast.success('Password reset successfully.');nav('/login');}catch(err){toast.error(err.friendlyMessage||'Reset link is invalid or expired.');}finally{setLoading(false)}};return <section className="scj-section"><div className="container"><div className="row justify-content-center"><div className="col-lg-5"><div className="scj-card p-4 p-md-5"><h1 className="h3 fw-bold">Set New Password</h1><form onSubmit={submit}><input className="form-control my-4" type="password" minLength="6" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password"/><Button className="w-100" type="submit" loading={loading}>Reset Password</Button></form><p className="text-center mt-3 mb-0"><Link to="/login">Back to Login</Link></p></div></div></div></div></section>}
